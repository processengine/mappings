# Release notes 2.1.0

- Added limited array DSL: `collect`, `count`, `existsAny`, `existsAll`, `pickFirst`.
- Added prepared artifact `v2` with compiled execution plan.
- Added legacy `v1` execution compatibility path.
- Expanded docs, examples, schema, and tests for array DSL.
