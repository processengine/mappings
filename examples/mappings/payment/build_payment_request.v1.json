{
  "mappingId": "request.build_checkout_payload.v1",
  "sources": {
    "order": "object",
    "channel": "object"
  },
  "output": {
    "request.orderId": { "from": "sources.order.id" },
    "request.amount": { "from": "sources.order.total.amount" },
    "request.currency": { "from": "sources.order.total.currency" },
    "request.contactEmail": { "from": "sources.order.contact.email" },
    "request.channel": { "from": "sources.channel.name" },
    "flags.isOnline": { "equals": ["sources.channel.name", "online"] },
    "flags.hasContactEmail": { "exists": "sources.order.contact.email" },
    "meta.mappingId": { "literal": "request.build_checkout_payload.v1" }
  }
}
